# PostgreSQL w/Express App Tutorial

## Setup

- Create a pool where you will have a 'pool' of connections to use for your app.
- Make sure to use parameterization when querying to prevent SQL injections.